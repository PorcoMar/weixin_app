<!--<view>这是订单页面</view>-->
<view class="section">
  <!--循环创立订单列表-->
  <view class="flex-wrp bac" style="flex-direction:column;" wx:for="{{array}}">
  <!--订单和门店详情-->
    <view class="flex-item shopList" bindtap="orderDetail" id="{{item.payStatus}}">
        <!--门店logo-->
        <view class="logo">
          <image src="{{item.logo}}" class="image" mode="scaleToFill"></image>
        </view>
        <!--详情-->
        <view class="shopOrder">
          <!--门店-->
          <view class="shop">
            <view class="shopDetail">
              <view class="shopName">{{item.shopName}}</view>
              <view class="orderTime">{{item.orderTime}}</view>
            </view>
            <view class="status">{{item.payStatus}}</view>
          </view>
          <!--订单--> 
            <block wx:if="{{item.payStatus === '已付款'}}">
            <view class="hasPay order" wx:for="{{item.orderDetail}}" wx:for-index="idx">
              <block wx:if="{{idx == 0}}">
                <text class="orderName">孕后修身保养服务</text>
                <text class="note">已完成</text>
              </block>
              <block wx:if="{{idx == 1}}">
                <text class="orderName">孕后修身保养服务</text>
                <text class="note green">已完成</text>
              </block>
              <block wx:if="{{idx == 2}}">
                <text class="orderName">孕后修身保养服务</text>
                <text class="note red">已完成</text>
              </block>
              <block wx:elif="{{idx == 3}}">
                <text>...</text>
              </block>
            </view>
            </block>
            <block wx:else>
            <view class="order"  wx:for="{{item.orderDetail}}" wx:for-index="idx" >
              <block wx:if="{{idx <= 2}}">
                <text class="orderName">Cocodomer面部护理1</text>
                <text class="note">¥5000</text>
              </block>
              <block wx:elif="{{idx == 3}}">
                <text>...</text>
              </block>
            </view>
            </block>
          <!--总额-->
          <view class="total" wx:if="{{item.payStatus === '待付尾款'}}">
            <text class="service">已支付  </text>
            <text class="cost">¥ 5196</text>
            <text class="balance">还需支付尾款</text>
            <text class="price"> ¥ 5196</text>
          </view>
          <view class="total" wx:elif="{{item.payStatus !== '已过期'}}">
            <text class="service">共{{item.orderTotal}}项服务</text>
            <text class="combind">合计</text>
            <text class="price"> ¥ {{item.combind}}</text>
          </view>
        </view>
    </view>
  <!--订单操作-->
    <view class="flex-item operate" wx:if="{{item.payStatus !== '已过期'}}">
      <block wx:for="{{item.operate}}" wx:for-item="itm">
          <view class="orderOperate red" wx:if="{{itm === '立即支付'}}">{{itm}}</view>
          <view class="orderOperate" wx:else>{{itm}}</view>       
      </block>
    </view>
    <view class="flex-item operate" wx:else>      
          <text class="pay">需支付 ¥ 666</text>     
    </view>
  </view>
</view>